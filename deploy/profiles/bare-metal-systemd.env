# Perfil recomendado para despliegue bare-metal con systemd.
# Copiar a /opt/reco-trading/.env y ajustar secretos.

# Servicios
ORCHESTRATOR_CMD="python scripts/live_trading.py"
TRADING_WORKER_CMD="python scripts/live_trading.py"
EVOLUTION_WORKER_CMD="python scripts/live_trading.py"
ARCHITECTURE_SEARCH_WORKER_CMD="python -c 'import time; print(\"architecture search not implemented\"); time.sleep(3600)'"
SELF_HEALING_WORKER_CMD="python -c 'import time; print(\"self healing loop not implemented\"); time.sleep(3600)'"

# Dependencias
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432
